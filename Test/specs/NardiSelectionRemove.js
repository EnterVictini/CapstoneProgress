import { expect } from '@wdio/globals'
import { browser } from '@wdio/globals'
import NardiProducts from '../pageobjects/10-functions.js'
import NardiRemove from '../pageobjects/NardiSelectionRemove.js'


/** describe('Nardi Selection Remove', () => {
    it('Test', async () => {
        await NardiRemove.open();
        await NardiProducts.NardiSelections();
        await NardiRemove.NardiSelectionRemove();

        
    })
})
*/
